# ML dependencies — requires Python 3.9-3.12 (NOT 3.13/3.14)
# Install: pip install -r requirements-ml.txt

# Alphabet CNN (ASL.h5 — Keras)
tensorflow-cpu>=2.15.0,<2.17.0
Pillow>=10.0.0
numpy>=1.24.0

# WLASL Transformer (keypoint-based word recognition)
torch>=2.0.0
# mediapipe not needed on backend — keypoint extraction happens in browser
